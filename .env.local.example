# Copy to .env.local and fill in. The DSN is safe to expose to the browser.
NEXT_PUBLIC_SENTRY_DSN=

# Only needed for source-map upload at build time (CI):
# SENTRY_ORG=
# SENTRY_PROJECT=
# SENTRY_AUTH_TOKEN=
